faster-whisper>=1.1
numpy
sounddevice
keyboard
pyperclip

# DLLs de CUDA que faster-whisper (ctranslate2) necesita en Windows.
# Se instalan dentro del venv; transcriptor.py expone sus rutas
# con os.add_dll_directory() antes de importar faster_whisper.
nvidia-cublas-cu12 ; sys_platform == "win32"
nvidia-cudnn-cu12>=9,<10 ; sys_platform == "win32"
