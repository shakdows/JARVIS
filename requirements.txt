faster-whisper>=1.1
numpy
sounddevice
keyboard
pyperclip
pyttsx3

# DLLs de CUDA que faster-whisper (ctranslate2) necesita en Windows.
# Se instalan dentro del venv; transcriptor.py expone sus rutas
# con os.add_dll_directory() antes de importar faster_whisper.
# Versiones fijadas: son las que ctranslate2 4.5+ espera (CUDA 12.4 + cuDNN 9.1)
# y las que tienen instalador para Windows comprobado.
nvidia-cublas-cu12==12.4.5.8 ; sys_platform == "win32"
nvidia-cudnn-cu12==9.1.0.70 ; sys_platform == "win32"
